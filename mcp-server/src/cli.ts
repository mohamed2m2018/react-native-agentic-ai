#!/usr/bin/env node

/**
 * @mobileai/mcp-server
 * MCP bridge between AI editors (Antigravity, Claude Desktop) and your React Native app.
 *
 * Usage:
 *   npx @mobileai/mcp-server
 *   MCP_PORT=3100 WS_PORT=3101 npx @mobileai/mcp-server
 */

import './index.js';

// Banner is printed inside index.ts at startup
